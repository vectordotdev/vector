receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:5318

exporters:
  file:
    path: /output/opentelemetry-native/collector-file-exporter.log
  debug:
    verbosity: detailed

service:
  pipelines:
    logs:
      receivers: [otlp]
      exporters: [file, debug]
