data_dir: /var/lib/vector

sources:
  in:
    type: http_server
    address: 0.0.0.0:8080
    decoding:
      codec: json
    acknowledgements:
      enabled: true

  metrics:
    type: internal_metrics
    scrape_interval_secs: 1

sinks:
  out:
    type: vector
    inputs: [in]
    address: "tail:6000"
    compression: none
    # Aggressive keepalive so the crash-and-recover path is exercised quickly: a pooled connection
    # to a killed `tail` is probed while idle and evicted within seconds instead of being reused.
    keepalive:
      interval_secs: 5
      timeout_secs: 5
    buffer:
      type: disk
      max_size: 8388608 # 8MiB (4 data files), small enough that a stalled reader fills it and blocks
      when_full: block
    acknowledgements:
      enabled: true

  prom:
    type: prometheus_exporter
    inputs: [metrics]
    address: 0.0.0.0:9598
