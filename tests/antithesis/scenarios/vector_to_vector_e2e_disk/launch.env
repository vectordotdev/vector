# Per-scenario settings for ../launch.sh. Only the bits that differ from every
# other scenario live here; the launcher supplies the rest.
SCENARIO_TEST_NAME=vector_to_vector_e2e_disk
SCENARIO_DESCRIPTION="disk_v2 conservation under crash/hang/throttle of head and tail"
SCENARIO_FAULT_NODES="head tail"
