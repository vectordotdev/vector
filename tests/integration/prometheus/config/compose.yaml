version: "3"

services:
  prometheus:
    image: docker.io/prom/prometheus:${CONFIG_PROMETHEUS}
    command: --config.file=/etc/vector/prometheus.yaml
    volumes:
      - ../data/prometheus.yaml:/etc/vector/prometheus.yaml:ro

networks:
  default:
    name: ${VECTOR_NETWORK}
    external: true
