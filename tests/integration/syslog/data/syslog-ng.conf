@version: current

options {
    chain_hostnames(off);
    use_dns(no);
    keep_hostname(yes);
    create_dirs(yes);
};

# Write the fields syslog-ng parsed out of each message as one JSON object per
# line, so tests verify that the receiver understood the message rather than
# just that the bytes arrived.
template parsed {
    template("$(format-json --scope none facility=$FACILITY_NUM severity=$LEVEL_NUM hostname=$HOST app_name=$PROGRAM proc_id=$PID msg_id=$MSGID structured_data=$SDATA message=$MSG raw=$RAWMSG)\n");
};

source s_udp {
    network(
        ip("0.0.0.0")
        port(5514)
        transport("udp")
        flags(syslog-protocol, store-raw-message)
    );
};

source s_tcp_line {
    network(
        ip("0.0.0.0")
        port(5515)
        transport("tcp")
        flags(syslog-protocol, store-raw-message)
    );
};

# The syslog() driver (unlike network()) parses RFC 6587 octet-counted
# framing, so these sources genuinely validate the frames Vector sends.
source s_tcp_octet {
    syslog(
        ip("0.0.0.0")
        port(5516)
        transport("tcp")
        flags(store-raw-message)
    );
};

source s_tcp_tls {
    syslog(
        ip("0.0.0.0")
        port(5517)
        transport("tls")
        flags(store-raw-message)
        tls(
            key-file("/etc/syslog-ng/tls/localhost.key.pem")
            cert-file("/etc/syslog-ng/tls/localhost.cert.pem")
            peer-verify("optional-untrusted")
        )
    );
};

destination d_udp {
    file("/var/log/vector-test/syslog-ng-udp.log" template(parsed));
};

destination d_tcp_line {
    file("/var/log/vector-test/syslog-ng-tcp-line.log" template(parsed));
};

destination d_tcp_octet {
    file("/var/log/vector-test/syslog-ng-tcp-octet.log" template(parsed));
};

destination d_tcp_tls {
    file("/var/log/vector-test/syslog-ng-tcp-tls.log" template(parsed));
};

log { source(s_udp); destination(d_udp); };
log { source(s_tcp_line); destination(d_tcp_line); };
log { source(s_tcp_octet); destination(d_tcp_octet); };
log { source(s_tcp_tls); destination(d_tcp_tls); };
