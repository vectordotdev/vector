*
!benches
!lib
!proto
!src
!tests
!build.rs
!Cargo.lock
!Cargo.toml
!rust-toolchain.toml
!scripts
!vdev

# Exclude target directories to avoid copying build artifacts
target

# `*` does not match dotfiles, so exclude them explicitly
.git
.github
.gitignore
