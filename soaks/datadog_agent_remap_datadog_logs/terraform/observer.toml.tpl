startup_delay_seconds = 75
prometheus = "http://prometheus:9090"
query = "${query}"
experiment_name = "${experiment_name}"
vector_id = "${vector_id}"
capture_path = "/captures/${experiment_name}-${vector_id}.captures"