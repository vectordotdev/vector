syntax = "proto3";

package record.proto;

message Record {
  oneof event {
    Log log = 1;
  }
}

message Value {
  bytes data = 1;
  bool explicit = 2;
}

message Log {
  map<string, Value> structured = 1;
}
