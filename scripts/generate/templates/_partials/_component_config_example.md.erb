{% code-tabs %}
{% code-tabs-item title="vector.toml (example)" %}
```toml
<%= config_example(component.options.to_h.values.sort, path: "sinks.my_#{component.id}_id") %>
```
{% endcode-tabs-item %}
{% code-tabs-item title="vector.toml (schema)" %}
```toml
<%= config_schema(component.options.to_h.values.sort, path: "sinks.<sink-id>") %>
```
{% endcode-tabs-item %}
{% code-tabs-item title="vector.toml (specification)" %}
```toml
<%= config_spec(component.options.to_h.values.sort, path: "sinks.#{component.name}") %>
```
{% endcode-tabs-item %}
{% endcode-tabs %}