1. ### Configure Vector

<%= command_create_config(tutorial.config_path, source: source).indent(3) %>

2. ### Start the Vector container

   ```bash
   docker run \
<%= tutorial.start_command_lines.join(" \\\n").indent(5) %>
   ```

   <%- if tutorial.start_command_explanations.any? -%>
   <CodeExplanation>

   * <%= tutorial.start_command_explanations.join("\n   * ") %>

   </CodeExplanation>
   <%- end -%>

   That's it! Simple and to the point. Hit `ctrl+c` to exit.
