The `<%= component.name %>` <%= component.type %> <%= component_short_description(component).continuize %>
<% if component.sink? && component.batching? -%>
Batches are flushed via the [`batch_size`](#batch_size) or
[`batch_timeout`](#batch_timeout) options. You can learn more in the [buffers &
batches](#buffers--batches) section.
<%- end -%>
<%- if examples.any? -%>
For example:

<%- if examples.size > 1 -%>

<Tabs
  block={true}
  defaultValue="<%= examples.first.label.slugify %>"
  values={<%= examples.collect { |e| {label: e.label, value: e.label.slugify} }.to_json %>}>
<%- end -%>
<%- examples.each do |example| -%>
<%- if examples.size > 1 -%>

<TabItem value="<%= example.label.slugify %>">
<%- end -%>

<%= example.body %>
<%- if examples.size > 1 -%>

</TabItem>
<%- end -%>
<%- end -%>
<%- if examples.size > 1 -%>
</Tabs>
<%- end -%>

<%- elsif fields -%>
For example:

<%= fields_example(fields, root_key: output.global_log_schema_key) %>
<%- end -%>
<%- if fields.any? -%>

More detail on the output schema is below.

<%= fields(fields, heading_depth: heading_depth) %>
<%- end -%>
