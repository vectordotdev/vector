<HighlightItems
  groupBy={<%= group_by.to_json %>}
  headingDepth={<%= heading_depth.to_json %>}
  items={<%= highlight_maps.to_json %>}
  size={<%= size.to_json %>}
  timeline={<%= timeline.to_json %>} />
