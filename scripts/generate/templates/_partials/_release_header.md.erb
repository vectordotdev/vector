---
title: <%= "#{release.title} Release Notes".to_json %>
codename: <%= release.codename.to_json %>
date: <%= release.date.to_json %>
description: <%= "#{release.title} - #{release.codename} - Release Notes".to_json %>
version: <%= release.version.to_json %>
---

<%- if release.description.strip != "" -%>
<%= release.description %>
<%- else -%>
The [Vector team][pages.community#team] is pleased to release
<%= release.version %>. This is a [<%= release.type %>](<%= release.type_url %>)
release. Checkout the [Highlights](#highlights), [Changelog](#changelog), and
[What's Next](#whats-next) section for more detail.
<%- end -%>
