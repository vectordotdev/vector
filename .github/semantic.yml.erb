# Always validate the PR title, and ignore the commits
titleOnly: true

types:
  - chore               # An internal change that should not be communicated to Vector users.
  - docs                # A change relating to Vector's documentation.
  - enhancement         # Anything that enhances it's subject.
  - feat                # An entirely new feature, such as a new platform, source, transform, or sink.
  - fix                 # A bug fix.
  - perf                # A change that enhancements performance.

scopes:

  - new source
  - new transform
  - new sink

  # domains
  <%- metadata.domains.each do |domain| -%>
  - <%= domain.name.downcase %>
  <%- end -%>

  # platforms
  <%- metadata.platforms.each do |platform|  -%>
  - <%= platform.name.downcase %> platform
  <%- end -%>

  # providers
  <%- metadata.service_providers.each do |service_provider| -%>
  - <%= service_provider.downcase %> provider
  <%- end -%>

  # sources
  <%- metadata.sources.to_h.values.sort.each do |source| -%>
  - <%= source.name.downcase %> source
  <%- end -%>

  # transforms
  <%- metadata.transforms.to_h.values.sort.each do |transform| -%>
  - <%= transform.name.downcase %> transform
  <%- end -%>

  # sinks
  <%- metadata.sinks.to_h.values.sort.each do |sink| -%>
  - <%= sink.name.downcase %> sink
  <%- end -%>
