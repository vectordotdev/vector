name: generate

on:
  push:
    branches:
      - master
    paths:
      - "**/*.md.erb"
      - ".meta/**"
      - "scripts/generate.rb"

  pull_request:
    paths:
      - "**/*.md.erb"
      - ".meta/**"
      - "scripts/generate.rb"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: make check-generate
