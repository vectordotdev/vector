# WARNING! THIS FILE IS AUTOGENERATED! DO NOT EDIT!
# Orginal file: https://github.com/timberio/vector-management/github/semantic_pull_requests.tf
# ------------------------------------------------------------------------------
#
# Configuration for probot/semantic-pull-requests GitHub check bot
#
# Installation/configuration docs here
# https://github.com/probot/semantic-pull-requests#configuration
#
# Always validate the PR title, and ignore the commits
titleOnly: true

types:
  - chore               # An internal change this is not observable by users.
  - docs                # Any user observable documentation change.
  - enhancement         # Any user observable enhancement to an existing feature.
  - feat                # Any user observable new feature, such as a new platform, source, transform, or sink.
  - fix                 # Any user observable bug fix.
  - perf                # Any user observable performance improvement.
  - status              # A status change to a Vector component.

scopes:
  - new source
  - new transform
  - new sink

  # domains
  - administration # Anything related to administration/operation
  - architecture # Anything related to architecture
  - auth # Anything related to authentication/authorization
  - buffers # Anything related to Vector's memory/disk buffers
  - ci # Anything related to Vector's CI environment
  - cli # Anything related to Vector's CLI
  - codecs # Anything related to Vector's codecs (encoding/decoding)
  - compression # Anything related to compressing data within Vector
  - config # Anything related to configuring Vector
  - data model # Anything related to Vector's internal data model
  - deps # Anything related to Vector's dependencies
  - dev # Anything related to Vector's development environment
  - enriching # Anything related to enriching Vector's events with context data
  - extensions # Anything related to extending Vector (WASM, plugins, etc.)
  - external docs # Anything related to Vector's external, public documentation
  - filtering # Anything related to filtering within Vector
  - healthchecks # Anything related to Vector's healtchecks
  - internal docs # Anything related to Vector's internal documentation
  - logs # Anything related to Vector's log events
  - mapping # Anything related to mapping data within Vector
  - marketing # Anything related to marketing Vector
  - metrics # Anything related to Vector's metrics events
  - networking # Anything related to Vector's networking
  - observability # Anything related to monitoring/observing Vector
  - parsing # Anything related to parsing within Vector
  - performance # Anything related to Vector's performance
  - platforms # Anything related to Vector's supported platforms
  - privacy # Anything related to privacy/compliance
  - processing # Anything related to processing Vector's events (parsing, merging, reducing, etc.)
  - releasing # Anything related to releasing Vector
  - reliability # Anything related to Vector's reliability
  - reload # Anything related to reloading Vector (updating configuration)
  - rfcs # Anything related to Vector's internal RFCs
  - security # Anything related to security
  - setup # Anything related to setting up or installing Vector
  - shutdown # Anything related to the shutdown of Vector
  - sinks # Anything related to the Vector's sinks
  - sources # Anything related to the Vector's sources
  - startup # Anything related to the startup of Vector
  - templating # Anything related to templating Vector's configuration values
  - tests # Anything related to Vector's internal tests
  - topology # Anything related to Vector's topology code
  - traces # Anything related to Vectors' trace events
  - transforms # Anything related to Vector's transform components

  # platforms
  - amazon-linux # Anything `amazon-linux` platform related
  - arm64 # Anything `arm64` platform related
  - centos # Anything `centos` platform related
  - debian # Anything `debian` platform related
  - docker # Anything `docker` platform related
  - dpkg # Anything `dpkg` platform related
  - heroku # Anything `heroku` platform related
  - homebrew # Anything `homebrew` platform related
  - kubernetes # Anything `kubernetes` platform related
  - macos # Anything `macos` platform related
  - msi # Anything `msi` platform related
  - nix # Anything `nix` platform related
  - nixos # Anything `nixos` platform related
  - raspbian # Anything `raspbian` platform related
  - rhel # Anything `rhel` platform related
  - rpm # Anything `rpm` platform related
  - ubuntu # Anything `ubuntu` platform related
  - windows # Anything `windows` platform related
  - x86_64 # Anything `x86_64` platform related

  # service providers
  - aws # Anything `aws` service provider related
  - azure # Anything `azure` service provider related
  - confluent # Anything `confluent` service provider related
  - datadog # Anything `datadog` service provider related
  - elastic # Anything `elastic` service provider related
  - gcp # Anything `gcp` service provider related
  - grafana # Anything `grafana` service provider related
  - heroku # Anything `heroku` service provider related
  - honeycomb # Anything `honeycomb` service provider related
  - humio # Anything `humio` service provider related
  - influxdata # Anything `influxdata` service provider related
  - logdna # Anything `logdna` service provider related
  - new relic # Anything `new relic` service provider related
  - papertrail # Anything `papertrail` service provider related
  - sematext # Anything `sematext` service provider related
  - splunk # Anything `splunk` service provider related
  - yandex # Anything `yandex` service provider related

  # sources
  - docker # Anything `docker` source related
  - file # Anything `file` source related
  - generator # Anything `generator` source related
  - http # Anything `http` source related
  - journald # Anything `journald` source related
  - kafka # Anything `kafka` source related
  - kubernetes_logs # Anything `kubernetes_logs` source related
  - logplex # Anything `logplex` source related
  - prometheus # Anything `prometheus` source related
  - socket # Anything `socket` source related
  - splunk_hec # Anything `splunk_hec` source related
  - statsd # Anything `statsd` source related
  - stdin # Anything `stdin` source related
  - syslog # Anything `syslog` source related
  - vector # Anything `vector` source related

  # transforms
  - add_fields # Anything `add_fields` transform related
  - add_tags # Anything `add_tags` transform related
  - ansi_stripper # Anything `ansi_stripper` transform related
  - aws_ec2_metadata # Anything `aws_ec2_metadata` transform related
  - coercer # Anything `coercer` transform related
  - concat # Anything `concat` transform related
  - dedupe # Anything `dedupe` transform related
  - filter # Anything `filter` transform related
  - geoip # Anything `geoip` transform related
  - grok_parser # Anything `grok_parser` transform related
  - json_parser # Anything `json_parser` transform related
  - log_to_metric # Anything `log_to_metric` transform related
  - logfmt_parser # Anything `logfmt_parser` transform related
  - lua # Anything `lua` transform related
  - merge # Anything `merge` transform related
  - reduce # Anything `reduce` transform related
  - regex_parser # Anything `regex_parser` transform related
  - remap # Anything `remap` transform related
  - remove_fields # Anything `remove_fields` transform related
  - remove_tags # Anything `remove_tags` transform related
  - rename_fields # Anything `rename_fields` transform related
  - sampler # Anything `sampler` transform related
  - split # Anything `split` transform related
  - swimlanes # Anything `swimlanes` transform related
  - tag_cardinality_limit # Anything `tag_cardinality_limit` transform related
  - tokenizer # Anything `tokenizer` transform related
  - wasm # Anything `wasm` transform related

  # sinks
  - aws_cloudwatch_logs # Anything `aws_cloudwatch_logs` sink related
  - aws_cloudwatch_metrics # Anything `aws_cloudwatch_metrics` sink related
  - aws_kinesis_firehose # Anything `aws_kinesis_firehose` sink related
  - aws_kinesis_streams # Anything `aws_kinesis_streams` sink related
  - aws_s3 # Anything `aws_s3` sink related
  - blackhole # Anything `blackhole` sink related
  - clickhouse # Anything `clickhouse` sink related
  - console # Anything `console` sink related
  - datadog_logs # Anything `datadog_logs` sink related
  - datadog_metrics # Anything `datadog_metrics` sink related
  - elasticsearch # Anything `elasticsearch` sink related
  - file # Anything `file` sink related
  - gcp_cloud_storage # Anything `gcp_cloud_storage` sink related
  - gcp_pubsub # Anything `gcp_pubsub` sink related
  - gcp_stackdriver_logs # Anything `gcp_stackdriver_logs` sink related
  - honeycomb # Anything `honeycomb` sink related
  - http # Anything `http` sink related
  - humio_logs # Anything `humio_logs` sink related
  - influxdb_logs # Anything `influxdb_logs` sink related
  - influxdb_metrics # Anything `influxdb_metrics` sink related
  - kafka # Anything `kafka` sink related
  - logdna # Anything `logdna` sink related
  - loki # Anything `loki` sink related
  - new_relic_logs # Anything `new_relic_logs` sink related
  - papertrail # Anything `papertrail` sink related
  - prometheus # Anything `prometheus` sink related
  - pulsar # Anything `pulsar` sink related
  - sematext_logs # Anything `sematext_logs` sink related
  - socket # Anything `socket` sink related
  - splunk_hec # Anything `splunk_hec` sink related
  - statsd # Anything `statsd` sink related
  - vector # Anything `vector` sink related
