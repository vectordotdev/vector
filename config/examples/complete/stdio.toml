# STDIO Example
# ------------------------------------------------------------------------------
# A simple STDIN / STDOUT example. This script is used in the getting started
# guide:
#
# https://docs.vector.dev/setup/getting-started/sending-your-first-event

[sources.in]
    type = "stdin"

[sinks.out]
    inputs = ["in"]
    type = "console"
