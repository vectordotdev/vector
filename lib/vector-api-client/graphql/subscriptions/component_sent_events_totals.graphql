subscription ComponentSentEventsTotalsSubscription($interval: Int!) {
    componentSentEventsTotals(interval: $interval) {
        componentId
        outputs {
            outputId
            sentEventsTotal {
                sentEventsTotal
            }
        }
        metric {
            sentEventsTotal
        }
    }
}
