subscription ComponentSentBytesTotalsSubscription($interval: Int!) {
    componentSentBytesTotals(interval: $interval) {
        componentId
        metric {
            sentBytesTotal
        }
    }
}
