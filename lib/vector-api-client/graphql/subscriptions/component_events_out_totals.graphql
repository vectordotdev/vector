subscription ComponentEventsOutTotalsSubscription($interval: Int!) {
    componentEventsOutTotals(interval: $interval) {
        pipelineId
        componentId
        metric {
            eventsOutTotal
        }
    }
}
