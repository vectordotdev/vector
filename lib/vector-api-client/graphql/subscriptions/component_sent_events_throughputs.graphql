subscription ComponentSentEventsThroughputsSubscription($interval: Int!) {
    componentSentEventsThroughputs(interval: $interval) {
        componentId
        outputs {
            outputId
            throughput
        }
        throughput
    }
}
