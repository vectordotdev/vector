subscription AllComponentEventsProcessedTotalsSubscription($interval: Int!) {
    allComponentEventsProcessedTotals(interval: $interval) {
        name
        metric {
            eventsProcessedTotal
        }
    }
}
