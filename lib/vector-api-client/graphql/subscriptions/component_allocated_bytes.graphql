subscription ComponentAllocatedBytesSubscription ($interval: Int!) {
    componentAllocatedBytes(interval: $interval)  {
        componentId
        metric {
            allocatedBytes
        }
    }
}
