# result:
# error[E652]: only objects can be merged
#   ┌─ :3:1
#   │
# 3 │ x |= 32
#   │ ^    ^^ this expression must resolve to an object
#   │ │
#   │ this expression must resolve to an object
#   │
#   = see language documentation at https://vrl.dev

x = "oh no"
x |= 32
