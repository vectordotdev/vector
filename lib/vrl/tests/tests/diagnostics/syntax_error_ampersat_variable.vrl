# result:
#
# error[E203]: syntax error
#   ┌─ :2:1
#   │
# 2 │ @timestamp = now()
#   │ ^^^^^^^^^^
#   │ │
#   │ unexpected syntax token: "PathField"
#   │ expected one of: "\n", "!", "(", ")", ";", "[", "]", "_", "abort", "float literal", "function call", "identifier", "integer literal", "raw string literal", "regex literal", "string literal", "timestamp literal", "{", "|", "|", "}", "path literal"
#   │
#   = see language documentation at https://vrl.dev
#   = try your code in the VRL REPL, learn more at https://vrl.dev/examples

@timestamp = now()
