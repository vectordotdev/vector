#result:
# warning[E801]: the "remove_metadata_field" function is deprecated
#   ┌─ :3:1
#   │
# 3 │ remove_metadata_field(.foo.bar)
#   │ ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ this is deprecated
#   │
#   = try: using the metadata path syntax instead
#   =
#   =     del(%foo.bar)
#   =
#   = see language documentation at https://vrl.dev
#   = try your code in the VRL REPL, learn more at https://vrl.dev/examples


remove_metadata_field(.foo.bar)
