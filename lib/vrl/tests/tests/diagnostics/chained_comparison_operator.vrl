# result:
#
# error[E650]: comparison operators can't be chained together
#   ┌─ :2:7
#   │
# 2 │ 2 < 3 < 4
#   │       ^
#   │
#   = see documentation about comparisons at https://vrl.dev/expressions/#comparison
#   = see language documentation at https://vrl.dev
#
# error[E650]: comparison operators can't be chained together
#   ┌─ :3:15
#   │
# 3 │ true == false == true
#   │               ^^
#   │
#   = see documentation about comparisons at https://vrl.dev/expressions/#comparison
#   = see language documentation at https://vrl.dev

2 < 3 < 4
true == false == true
