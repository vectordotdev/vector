# result:
#
# error[E122]: type mismatch in enumeration return type
#   ┌─ :2:21
#   │
# 2 │ map_keys({}) -> |k| { null }
#   │                     ^^^^^^^^
#   │                     │
#   │                     block returns invalid value type
#   │                     received: null
#   │                     expected: string
#   │
#   = see language documentation at https://vrl.dev

map_keys({}) -> |k| { null }
