# result:
#
# error[E111]: missing closure
#   ┌─ :2:1
#   │
# 2 │ for_each({})
#   │ ^^^^^^^^^^^^ this function expects a closure
#   │
#   = example: for_each({ "one" : 1, "two": 2 }) -> |key, value| { .foo = to_int!(.foo) + int!(value) }
#   = see language documentation at https://vrl.dev

for_each({})
