# result: [1, [0], [0], ["true", 0]]

# NOTE: the function calls in this test are needed to validate the type
# definition results.

v1 = "foo"
v1 = 1
ceil(v1)

v2 = ["true"]
v2 = [0]
ceil(v2[0])

v3 = ["true"]
v3[0] = 0
ceil(v3[0])

v4 = ["true"]
v4[1] = 0
upcase(v4[0])

[v1, v2, v3, v4]
