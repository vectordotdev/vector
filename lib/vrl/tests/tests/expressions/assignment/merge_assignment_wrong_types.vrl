# result: ~
# only objects can be merged
# only objects can be merged
# only objects can be merged
# only objects can be merged
# only objects can be merged

. = { "ook": 23 }

. |= 32
. |= "oops"
. |= [ "oh", "dear" ]

x = "oh no"

x |= { "ook": 32 }
x |= 32
