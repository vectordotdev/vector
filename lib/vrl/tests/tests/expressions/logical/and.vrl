# result: [true, false, false, false, "nope", "nope"]

[
    true && true,
    false && true,
    true && false,
    false && false,

    # fallible
    true && "foo" ?? "nope",
    downcase(.thing) && .thing ?? "nope"
]
