[package]
name = "precompiled"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["dylib"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
vrl-compiler = { path = "../.." }
lookup = { path = "../../../../lookup" }
