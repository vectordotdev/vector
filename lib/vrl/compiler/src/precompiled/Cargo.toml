[package]
name = "precompiled"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
lookup = { path = "../../../../lookup" }
shared = { path = "../../../../shared" }
vrl-compiler = { path = "../..", default-features = false }

[workspace]
