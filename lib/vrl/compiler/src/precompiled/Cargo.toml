[package]
name = "precompiled"
version = "0.1.0"
edition = "2021"

[dependencies]
lookup = { path = "../../../../lookup" }
shared = { path = "../../../../shared" }
vrl-compiler = { path = "../..", default-features = false }

[workspace]
