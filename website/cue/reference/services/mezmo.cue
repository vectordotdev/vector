package metadata

services: mezmo: {
	name:     "Mezmo (formerly LogDNA)"
	thing:    "a \(name) account"
	url:      urls.mezmo
	versions: null

	description: "[Mezmo](\(urls.mezmo)) is a log management system that allows engineering and DevOps to aggregate all system, server, and application logs into one platform. Collect, monitor, store, tail, and search application logs in with one command-line or web interface."
}
