package metadata

remap: syntax: whitespace: {
	title:       "Whitespace"
	description: """
		Whitespace is any non-empty string as defined by the [Unicode `White_Space` property](\(urls.unicode_whitespace)).

		VRL is a "free-form" language, meaning that all forms of whitespace serve only to separate tokens in the
		grammar, and have no semantic significance.
		"""
}
