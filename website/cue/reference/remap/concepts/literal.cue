remap: concepts: literal: {
	title:       "Literal"
	description: """
		As in most other languages, [literals](\(urls.vrl_literals)) in VRL are values written
		exactly as they are meant to be interpreted. Literals include things like strings, Booleans,
		and integers.
		"""
}
