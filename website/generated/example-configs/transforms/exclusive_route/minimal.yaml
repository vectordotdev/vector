transforms:
  my_transform_id:
    type: exclusive_route
    inputs:
      - my-source-or-transform-id
    routes:
      - condition:
          source: exists(.foo) && exists(.bar)
          type: vrl
        name: foo-and-bar-exist
