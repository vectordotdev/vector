transforms:
  my_transform_id:
    type: route
    inputs:
      - my-source-or-transform-id
    route:
      foo-does-not-exist:
        source: "!exists(.foo)"
        type: vrl
      foo-exists:
        source: exists(.foo)
        type: vrl
