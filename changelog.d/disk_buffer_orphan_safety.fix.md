Vector now detects orphaned disk buffers, validates that configured buffers can grow within actually usable disk space, and logs buffer reuse and background drain progress during configuration reloads.

authors: Jansen-w
