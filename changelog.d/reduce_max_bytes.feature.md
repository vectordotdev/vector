The `reduce` transform now supports a `max_bytes` configuration option. When set, Vector will flush the current group if an incoming event would cause the accumulated group size to exceed the limit, then start a new group with that event. Events that individually exceed the limit are still emitted as a group of one. A new `byte_limit_flushed_total` counter metric is incremented each time a flush is triggered by the byte limit.
