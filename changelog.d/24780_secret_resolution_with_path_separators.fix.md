Fixed an issue where directory secret backends failed to resolve secrets organized in subdirectories
(e.g., Kubernetes mounted secrets at paths like: `/secrets/my-secrets/username`)

authors: pront vparfonov
