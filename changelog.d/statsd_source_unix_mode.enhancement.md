The `statsd` source supports a `unix_mode` configuration option for Unix domain sockets, allowing selection between stream (`SOCK_STREAM`) and datagram (`SOCK_DGRAM`) modes. A `socket_file_mode` option is also available to set file permissions on the created Unix socket.

authors: []
