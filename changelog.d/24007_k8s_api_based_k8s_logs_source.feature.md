The `kubernetes_logs` source now supports collecting logs via the Kubernetes API as an alternative to file-based log collection. Enable this with the new `api_log` configuration option.

authors: titaneric
