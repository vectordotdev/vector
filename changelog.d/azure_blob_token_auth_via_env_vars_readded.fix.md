The `azure_blob` sink once again supports authenticating via tokens in environment variables.
This unblocks production use cases as connection_strings are not sufficiently secure for use in real environments.

authors: oganel
