The `retry_strategy` option for the `opentelemetry` sink (previously available as
`protocol.retry_strategy` in the nested format) is restored and available at the top level when
`protocol` is set to `http`.

authors: sakateka
