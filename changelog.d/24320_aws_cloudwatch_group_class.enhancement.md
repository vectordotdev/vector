Add the ability to set the log group class for the `aws_cloudwatch_logs` sink when creating the log group. Optional field that defaults to `standard` when omitted.  The log class can only be set during group creation and cannot be changed after.

authors: cahartma
