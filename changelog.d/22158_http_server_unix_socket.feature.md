Add support for listening on Unix Domain Sockets (UDS) in the `http_server` source via the new `socket` option, which groups the socket `path` with optional `file_mode`, `file_uid`, and `file_gid` settings.

authors: gobinathal
