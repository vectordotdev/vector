Add support for listening on Unix Domain Sockets (UDS) in the `http_server` source via the `socket_path`, `socket_file_mode`, `socket_file_uid`, and `socket_file_gid` configuration options.

authors: gobinathal
