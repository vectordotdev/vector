The `opentelemetry` sink still accepts the legacy nested `protocol.*` configuration format as a
deprecated fallback during migration. A warning is logged when the legacy format is used.

authors: sakateka
