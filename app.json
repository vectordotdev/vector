{
    "name": "Vector",
    "description": "Vector is a lightweight tool for building observability pipelines. (With easy setup for CloudWatch!)",
    "repository": "https://github.com/timberio/vector",
    "logo": "https://vector.dev/press/vector-icon.png",
    "keywords": ["observability"],
    "buildpacks": [
        { "url": "https://github.com/timberio/vector-heroku-buildpack" }
    ],
    "env": {
        "VECTOR_TOML_URL": {
            "description": "The URL of the `vector.toml` you use to use.",
            "required": true
        },
        "AWS_ACCESS_KEY_ID": {
            "description": "An AWS key ID, if needed. (eg. For sinking to CloudWatch)",
            "required": false
        },
        "AWS_SECRET_ACCESS_KEY": {
            "description": "An AWS access key, if needed. (eg. For sinking to CloudWatch)",
            "required": false
        },
        "AWS_REGION": {
            "description": "An AWS region, if needed.",
            "required": false
        },
        "AWS_CLOUDWATCH_STREAM_NAME": {
            "description": "An AWS CloudWatch stream name, if needed.",
            "required": false
        },
        "AWS_CLOUDWATCH_GROUP_NAME": {
            "description": "An AWS CloudWatch stream group name, if needed.",
            "required": false
        }
    },
    "formation": {
        "web": {
            "quantity": 1,
            "size": "standard-1x"
        }
    }
}
