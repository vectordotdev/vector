remap2: features: native: {
	title: "Native"
	description: """
		VRL is native to Vector since both are written in Rust, and does not pay a penalty to receive and return
		Vector events. This makes VRL significantly faster than alternative runtimes, like Lua, where data must
		be serialized as it's passed to and from the runtime.
		"""
}
